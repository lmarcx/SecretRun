{
  "name": "@secret-run/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "backend:start": "cd nhost && docker compose up -d",
    "backend:stop": "cd nhost && docker compose down",
    "backend:logs": "cd nhost && docker compose logs -f --tail=100",
    "db:migrate": "cd nhost && nhost migrate apply",
    "db:seed": "cd nhost && docker compose exec -T postgres psql -U postgres -d secretrun -f /seeds/seeds.sql",
    "db:seed:full": "cd nhost && docker compose exec -T postgres psql -U postgres -d secretrun -f /seeds/secret_run_full_seed.sql",
    "db:seed:clear": "cd nhost && docker compose exec -T postgres psql -U postgres -d secretrun -f /seeds/secret_run_clear_seed.sql",
    "lint": "eslint nhost/functions --ext .ts"
  },
  "dependencies": {
    "graphql-request": "^7.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@typescript-eslint/eslint-plugin": "^8.26.0",
    "@typescript-eslint/parser": "^8.26.0",
    "eslint-config-prettier": "^10.1.1",
    "typescript": "^5.8.2"
  }
}
