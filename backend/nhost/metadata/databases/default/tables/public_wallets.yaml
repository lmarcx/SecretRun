table:
  name: wallets
  schema: public
object_relationships:
  - name: profile
    using:
      foreign_key_constraint_on: user_id
array_relationships:
  - name: ledger_entries
    using:
      foreign_key_constraint_on:
        column: wallet_id
        table:
          name: wallet_ledger
          schema: public
