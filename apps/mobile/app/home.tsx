import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function HomeScreen() {
  return (
    <PlaceholderScreen
      title="Home"
      subtitle="Discover your next secret event. Route reveal happens exactly 4 hours before start."
    />
  );
}
